const initialState = {
  items: [{ id: '0', title: '' }, { id: '1', title: '' }]
}

const items = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export { items }
